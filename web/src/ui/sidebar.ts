// Sidebar stub — will be fleshed out in a follow-up PR once the solver output
// is available from the WASM module.
export function renderSidebar(container: HTMLElement): void {
  container.innerHTML = "Solver output will appear here";
}
