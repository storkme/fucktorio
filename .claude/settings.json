{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "{ cmd=$(jq -r '.tool_input.command'); if echo \"$cmd\" | grep -q '^git commit'; then ruff check --fix . && ruff format . && git add -u; fi; } 2>/dev/null || true",
            "statusMessage": "Running ruff pre-commit..."
          }
        ]
      }
    ]
  }
}
